# ==========================================
# Multi-Agent Research Platform
# PRODUCTION DEPENDENCIES
# ==========================================
#
# Only packages directly imported by production
# code or essential for runtime are listed here.
#
# Transitive dependencies (torch, transformers,
# numpy, scikit-learn, onnxruntime, etc.) are
# resolved automatically by pip.
#
# Install:
#   pip install --no-cache-dir -r requirements-prod.txt
#
# ==========================================

# ── Web Framework ────────────────────────
fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.12.5
pydantic-settings==2.13.1
python-multipart==0.0.26

# ── LLM Inference ────────────────────────
groq==1.1.2

# ── Web Search & Content Fetching ────────
ddgs==9.14.0
duckduckgo_search==8.1.1
trafilatura==2.0.0
httpx==0.28.1

# ── Vector Store (Embedded ChromaDB) ────
chromadb==1.5.7
sentence-transformers==5.4.0

# ── Database (MongoDB via Motor) ─────────
motor==3.7.1
pymongo==4.17.0

# ── Environment & Configuration ──────────
python-dotenv==1.2.2
